package com.example.interviewstarter.work

import org.junit.Assert.assertTrue
import org.junit.Test

class UserSyncWorkerTest {
    @Test
    fun workerPolicyIsDocumented() {
        // Integration tests should use WorkManager's TestDriver and a real
        // test Application/Koin graph. This lightweight test keeps the
        // starter template runnable before device instrumentation setup.
        assertTrue(true)
    }
}
